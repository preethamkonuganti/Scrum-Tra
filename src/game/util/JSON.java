package game.util;

public class JSON {

    public static JSON parseString(String res){
        JSON json = new JSON();
        /**
         * Logic to parse String to JSON
         */
        return json;
    }
}
